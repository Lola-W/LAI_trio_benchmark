# Sample reading + masking selection for LAI benchmark test set.
masking_mode: "child_only"

# Select children from this population/superpopulation.
population: "CHS"
superpopulation: "EAS"

# First run target: chr22 with 11 CHS test children (existing 1 + 10 added).
n_samples: 11
seed_samples:
  - "HG00405"
