# Sample reading + masking selection for LAI benchmark test set.
masking_mode: "child_only"

# Use ALL/ALL to include every child in the ped-derived complete trio file.
population: "ALL"
superpopulation: "ALL"

# Legacy subset fields (ignored by current all-eligible selection logic).
n_samples: 0
seed_samples: []
